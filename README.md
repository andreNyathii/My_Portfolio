# Portfolio

Personal portfolio built with React and Vite. Still a work in progress.

## Setup

1. Clone the repo
2. Run `npm install`
3. Start the dev server with `npm run dev`

## Tech Stack
- React
- Vite
- Custom CSS (no Tailwind)
